The matlab `m` initial code comes from external acoustic post-doc.
Then quickly translated into Python and also Julia (by myself).

The initial code is buggy, even WRONG !
Therefore, the other ones too.
